panel "History" do
    table_for assigns[:versions].order(:id).reverse do
        column "Version number", :index
        column "Admin", :whodunnit
        column "Date Created", :created_at
        column "Save" do |version|
            link_to "save", :controller => "laplaya_files", :action=> "save", :version=> version.index
        end
    end
end

