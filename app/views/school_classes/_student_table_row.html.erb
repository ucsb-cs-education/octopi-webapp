<tr>
  <td><%= student.first_name %></td>
  <td><%= student.last_name %></td>
  <td><%= student.login_name %> <%= link_to 'edit', edit_admin_student_path(student) unless student.is_a?(TestStudent) %></td>
</tr>