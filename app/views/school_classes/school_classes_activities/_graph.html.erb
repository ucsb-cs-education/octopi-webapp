<%= column_chart (graph_info),
                 colors: ['#caffca', '#bce8f1', '#FFFFD5'],
                 max: @school_class.students.not_teststudents.count + (current_student ? 1 : 0),
                 stacked: true,
                 id: "progress-chart" %>