<%= column_chart (graph_info),
                 colors: ['#caffca', '#FFFFD5'],
                 max: @school_class.students.count,
                 stacked: true,
                 id: "progress-chart" %>