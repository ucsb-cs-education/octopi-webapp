$(function() {
    $("#classlist").append('<%= j render partial: 'student_table_row', locals: {student: @student} %>');
    $('#existing_student_list').find('option[value="<%= @student.id%>"]').remove();
});