<%= teacher.name %>
(<%= link_to 'remove', remove_teacher_school_class_path(@school_class,teacher.id), method: :delete, confirm: "Are you sure you want to remove #{teacher.name} as a teacher of this class?" %>)
<br>
