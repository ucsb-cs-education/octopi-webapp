$('#task-prerequisites').append("<%= escape_javascript(render partial: "pages/tasks/prereq_link", object: @dependency, as: :dependency ) %>");
$('#task_dependency_prerequisite_id option[value = <%= @dependency.prerequisite.id%>]').remove();