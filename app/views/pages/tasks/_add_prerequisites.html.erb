<%= simple_form_for [@page, @task_dependencies.build], html: {method: :post}, remote: true do |f| %>
    <%= f.input :prerequisite_id, collection: @relatable_tasks %>
    <%= f.submit "Add New", class: "add-dependant-button" %>
<% end %>