<%= simple_form_for [@page, @activity_dependencies.build], html: {method: :post}, remote: true do |f| %>
    <%= f.input :task_prerequisite_id, input_html:{class: 'input-sm'}, label: "Add new prerequisite", required: false, collection: @relatable_tasks %>
    <%= f.submit "Add New Prerequisite", class: 'btn btn-default btn-xs' %>
<% end %>