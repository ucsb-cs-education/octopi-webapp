# from /Users/dianafranklin/OctopiWebsite/octopi-webapp!
# see the status
git status
# get the latest copy
git pull
# commit the new changes
git commit -a 
# push the new changes to github
git push
# now push latest master to heroku
git checkout heroku
# check the status
git status
# now merge the two
git merge master
# push the changes to heroku
git push
# this deploys it
git push heroku heroku:master

